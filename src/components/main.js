import React from "react"
import CategoryColumn from "./categoryColumn";
import Category from "./category";

const Main = ({ children }) => (
    <div className="main">
        {children}
    </div>
);

export default Main;
